//setTimeout

// The setTimeout() method calls a function after a number of milliseconds.
//const myTimeout = setTimeout(fun, timein millisecond  5000);

let settime= setTimeout(greetings,3000);

function greetings(){
  a="anata ksukete";
  console.log(a)
  return a;
}
